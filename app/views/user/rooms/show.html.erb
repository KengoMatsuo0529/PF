<div class="container">
  <div class="row mx-auto my-4">
    <h2>チャット画面</h2>
  </div>

<div id="messages" room_id="<%= @room.id %>">
  <% @messages.update(user_checked: true) %>
  <%= render @messages %>
</div>

<form>
  <label>エンターキーで送信ができます</label><br>
  <input type="text" data-behavior="room_speaker">
</form>
<%= link_to "健康状態を報告する",new_user_user_health_path(health_center_id: @room.health_center_id), class:"user_health_-button" %>

<%= render '/messages/room_channel' %>
