<h2>保健所の検索</h2>

<p>お住まいの市町村を入れていただくことで保健所の検索が可能です</p>

<div>
  <%= form_tag(user_health_centers_search_path, method: :get) do %>
    <%= text_field_tag (:health_center) %>
    <%= submit_tag "検索" %>
  <% end %>
</div>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
     <th></th>
     <th>Title</th>
     <th>Opinion</th>
     <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
     <% @health_center.each do |health_center| %>
     <tr>
      <td>
       <%= link_to(health_center) do %>
       <%= attachment_image_tag(health_center, :profile_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
       <% end %>
      </td>
      <td><%= health_center %></td>
     </tr>
     <% end %>
  </tbody>
